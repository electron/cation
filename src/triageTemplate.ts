const issueParser = require('github-issue-parser');
import { Context } from 'probot';

export const triageBugReport = (components: string[], context: Context) => {};

export const triageFeatureRequest = (components: string[], context: Context) => {};

export const triageMASRejection = (components: string[], context: Context) => {};
